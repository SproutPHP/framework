<h2>Security Configuration Test</h2>
<p><strong>XSS Protection:</strong> {{ xss_enabled ? 'Enabled' : 'Disabled' }}</p>
<p><strong>XSS Mode:</strong> {{ xss_mode }}</p>
<p><strong>CSP Enabled:</strong> {{ csp_enabled ? 'Enabled' : 'Disabled' }}</p>
<p><strong>CSP Report Only:</strong> {{ csp_report_only ? 'Yes' : 'No' }}</p>
<p><strong>Environment:</strong> {{ env }}</p>
<p><strong>Debug Mode:</strong> {{ debug ? 'Enabled' : 'Disabled' }}</p>
<p style="color: {{ env == 'local' ? 'red' : 'green' }};">This text should be red in local environment</p> 